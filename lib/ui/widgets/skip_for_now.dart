import 'package:flutter/material.dart';

class SkipForNow extends StatelessWidget {
  const SkipForNow({super.key, required this.text, required this.skip});

  final String text;
  final VoidCallback skip;

  @override
  Widget build(BuildContext context) {
    return Ink(

    );
  }
}
